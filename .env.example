# Конфигурация GCP Cost Agent
# GCP Cost Agent Configuration

# ID вашего GCP проекта / Your GCP Project ID
GCP_PROJECT_ID=playground-332710

# Полное имя таблицы биллинга в BigQuery (формат: project.dataset.table)
# Full BigQuery billing table name (format: project.dataset.table)
BILLING_TABLE=playground-332710.billing.gcp_billing_export_v1_015E0F_53DB2E_EC3C6C

# URL MCP Toolbox сервера (по умолчанию локальный)
# MCP Toolbox server URL (default is local)
TOOLBOX_URL=http://127.0.0.1:5000

# Порт для Flask приложения (Cloud Run использует переменную PORT автоматически)
# Port for Flask app (Cloud Run sets PORT automatically)
PORT=8080
